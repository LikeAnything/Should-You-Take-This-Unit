// Should draw stuff later!
// Just code to set up the svg for now.
const svg = d3.select("body")
              .append("svg")
              .attr("height", "100%")
              .attr("width", "100%"),
      height = 500,
      width = 500,
      margin = {left: 50, right: 50, top: 40, bottom: 0}
